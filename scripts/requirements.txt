# Python dependencies for session-memory plugin

# MCP server (auto-installed by run-mcp-server.sh)
mcp>=1.0.0

# Semantic search (optional — keyword search works without these)
sentence-transformers>=2.2.0
torch>=2.0.0
